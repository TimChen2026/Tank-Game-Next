# 操作说明 — 坦克大战 (Tank Game Next)

## 1. 开始游戏

### 1.1 访问游戏

- **本地开发**：运行 `npm run dev`，打开 `http://localhost:3000`
- **生产环境**：部署到 Vercel 后访问你的部署地址

### 1.2 主菜单

首次加载游戏时，你将看到主菜单画面，包含：

- 游戏标题"坦克大战"
- 副标题 "BATTLE CITY"
- "按 ENTER 或点击开始" 提示
- 键盘操作提示（WASD / 方向键 + 空格）
- 手机端提示

### 1.3 开始游戏

按 **Enter** 键或点击游戏画布即可开始。

---

## 2. 操作方式

### 2.1 键盘控制

| 操作 | 按键 |
|------|------|
| 上移 | `W` 或 `↑`（上方向键） |
| 下移 | `S` 或 `↓`（下方向键） |
| 左移 | `A` 或 `←`（左方向键） |
| 右移 | `D` 或 `→`（右方向键） |
| 射击 | `Space`（空格键） |
| 开始 / 重新开始 | `Enter`（回车键） |

### 2.2 手机触控

在手机或平板上游戏时，屏幕底部会自动显示虚拟按键：

- **方向键**（左侧）：4 个方向按钮（▲ ▼ ◄ ►）
- **射击按钮**（右侧）：红色圆形按钮 🔥

> **注意**：小屏幕横屏模式（高度 < 500px）下会自动隐藏虚拟按键，以获得更好的游戏体验。

---

## 3. 游戏玩法

### 3.1 目标

- **胜利条件**：消灭全部 **20 辆敌人坦克**
- **失败条件**：基地（鹰旗）被摧毁，或失去全部 **3 条命**

### 3.2 地图地形

| 地形 | 外观 | 说明 |
|------|------|------|
| 空地 | 黑色 | 可通行的开阔地 |
| 砖墙 | 棕色砖块 | 可被子弹摧毁 |
| 钢墙 | 灰色金属 | 不可被子弹摧毁 |
| 水域 | 蓝色波浪 | 不可通行 |
| 草丛 | 绿色 | 可通行，隐藏在下方坦克 |
| 基地 | 橙色鹰旗 | 你的基地，必须保护 |

### 3.3 玩家

- **生命数**：共 3 条命（显示在左上角 HUD 中）
- **无敌时间**：复活后短暂无敌（闪烁护盾效果）
- **子弹限制**：同时最多发射 2 颗子弹

### 3.4 敌人

- **总数**：20 辆敌人坦克
- **同屏上限**：12 辆
- **刷新点**：地图左上、中上、右上
- **AI 行为**：随机移动，遇到障碍物改变方向，定期射击

### 3.5 HUD 信息

游戏画面顶部深色条显示：

- **生命**（左侧）：剩余生命数
- **敌人**（中间）：剩余敌人数量
- **得分**（右侧）：当前得分（每消灭一个敌人得 100 分）

---

## 4. 游戏状态

### 4.1 菜单状态

初始画面，显示游戏标题和开始提示。

### 4.2 游戏进行中

活跃的游戏状态 — 控制坦克、消灭敌人、保护基地。

### 4.3 游戏结束

触发条件：
- 基地（鹰旗）被摧毁
- 3 条命全部用完

"GAME OVER" 画面显示最终得分。按 **Enter** 键或点击重新开始。

### 4.4 胜利

触发条件：全部 20 辆敌人坦克被消灭。"胜利!" 画面显示得分。按 **Enter** 键或点击重新开始。

---

## 5. 用户系统

### 5.1 注册

1. 点击右上角导航栏的 **"注册"**
2. 输入用户名（2-50 个字符）和密码（至少 6 位）
3. 确认密码后点击 **"注册"**
4. 注册成功后自动登录并跳转到游戏

### 5.2 登录

1. 点击右上角导航栏的 **"登录"**
2. 输入用户名和密码
3. 点击 **"登录"**
4. 成功后跳转到游戏

### 5.3 登出

点击右上角导航栏的 **"退出登录"**

### 5.4 个人中心

1. 点击导航栏中的 **"个人中心"**
2. 查看游戏历史记录表格（包含场景、得分、结果、日期）
3. 点击 **"返回游戏"** 回到游戏

---

## 6. 排行榜

点击导航栏中的 **"排行榜"** 打开排行榜弹窗。

排行榜显示排名前 10 的玩家，按最高分排序，包括：
- 排名（金/银/铜色高亮）
- 指挥官（用户名）
- 最高分
- 场次

---

## 7. 游戏记录

游戏记录在胜利或失败时自动保存（仅限已登录用户）。

- 记录通过 `/api/game-records` 保存到数据库
- 可在 **个人中心** 查看历史记录
- 每条记录包含：场景、最终得分、结果（通关/失败）、游玩时间

---

## 8. 管理操作

### 8.1 初始化数据库

```bash
npm run init-db
```

该命令会在你的 PostgreSQL 数据库中创建 `users` 和 `game_records` 表。

### 8.2 环境变量

| 变量 | 说明 |
|------|------|
| `DATABASE_URL` | PostgreSQL 连接字符串 |
| `JWT_SECRET` | JWT 令牌签名密钥 |

### 8.3 可用脚本

| 脚本 | 说明 |
|------|------|
| `npm run dev` | 启动开发服务器 |
| `npm run build` | 生产构建 |
| `npm start` | 启动生产服务器 |
| `npm run init-db` | 初始化数据库表 |

---

## 9. 常见问题

### 9.1 画布无法渲染

- 确保浏览器支持 HTML5 Canvas
- 尝试刷新页面
- 检查浏览器控制台是否有错误

### 9.2 登录/注册错误

- 确保数据库已正确初始化
- 验证 `DATABASE_URL` 配置是否正确
- 检查 Neon 数据库是否可访问

### 9.3 游戏性能问题

- 关闭其他浏览器标签页释放资源
- 降低浏览器缩放级别
- 必要时在浏览器设置中关闭硬件加速

### 9.4 手机端按键不显示

- 确保使用触屏设备
- 尝试竖屏模式（小屏幕横屏会隐藏按键）
- 刷新页面

---

## 10. 技术说明

- 游戏使用 60fps 的 requestAnimationFrame 循环
- 地图渲染基于像素，使用 CSS `image-rendering: pixelated`
- 音效使用 Web Audio API（OscillatorNode）— 无需外部音频文件
- 认证通过 HTTP-only Cookie + JWT 实现
- 数据库查询使用参数化语句防止 SQL 注入